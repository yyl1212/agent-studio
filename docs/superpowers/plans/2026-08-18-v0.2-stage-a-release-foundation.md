# Agent Studio v0.2 阶段 A 发布基础实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 v0.2 源码开发者预览的治理文件、统一构建版本、不依赖 Docker 的快速验证与 GitHub Actions，为阶段 B 的官方节点开发建立稳定合并门禁。

**Architecture:** 新增只读的 `internal/buildinfo` 作为 CLI 与 API 的唯一构建身份来源；Makefile 把 Go 与 Web 快速检查拆成可并行目标，GitHub Actions 只复用这些目标。发布预检保持为可测试的 POSIX shell 脚本，治理文档与 GitHub 模板独立于运行时代码。

**Tech Stack:** Go 1.26.5、`runtime/debug`、`log/slog`、POSIX shell、Make、Node.js 24、Corepack、pnpm 10.34.5、GitHub Actions、Apache-2.0、Contributor Covenant 2.1。

**Spec:** `docs/superpowers/specs/2026-08-18-v0.2-source-preview-design.md`

## 全局约束

- 本计划只实施设计规格的阶段 A；不得新增 Retriever、Webhook、标签、GitHub Release、二进制或镜像发布。
- 开始执行前使用 `superpowers:using-git-worktrees`，从已同步的 `origin/main` 创建 `codex/v0.2-stage-a-release-foundation`；把本规格与本计划提交带入该分支。
- 所有 Go 构建、测试、生成和 vet 命令设置 `CGO_ENABLED=0`。
- Go 版本以 `go.mod` 的 Go 1.26 和 `toolchain go1.26.5` 为准。
- Node.js 使用 24.x；pnpm 固定为 10.34.5，安装必须使用 frozen lockfile。
- `make verify-quick` 和 GitHub CI 不得启动 Docker、PostgreSQL 或 Playwright。
- `make verify` 继续使用 Docker PostgreSQL，不能被快速目标改变语义。
- 不新增生产依赖；buildinfo、版本检查和 CI 只使用 Go 标准库、仓库现有依赖与 GitHub 官方/既定 Actions。
- 代码按 TDD 顺序开发：失败测试、最小实现、通过测试、独立提交。
- 不直接编辑生成文件；只能运行既有生成命令并检查漂移。
- 不删除、移动或写入用户现有 `.pnpm-store/`；只将根缓存目录加入 `.gitignore`。
- GitHub Private Vulnerability Reporting 当前实测为关闭状态。安全文档落地后必须取得用户对仓库设置变更的授权，再启用并复查；未启用时阶段 A 不能宣称完成。
- 不自动 push、开 PR、合并或修改标签；本地验证与代码审查通过后再向用户请求下一步授权。

---

## 文件地图

### 构建身份

| 文件 | 操作 | 职责 |
|---|---|---|
| `internal/buildinfo/buildinfo.go` | 新增 | 从 override、Go module build info 和 VCS settings 解析统一构建身份 |
| `internal/buildinfo/buildinfo_test.go` | 新增 | 锁定 devel、tag、revision、dirty 与 fallback 行为 |
| `internal/cli/app.go` | 修改 | `version` 命令消费 buildinfo |
| `internal/cli/app_test.go` | 修改 | 通过依赖注入验证稳定版本输出 |
| `apps/api/cmd/server/main.go` | 修改 | 服务启动日志输出同一 buildinfo |
| `apps/api/cmd/server/main_test.go` | 修改 | 验证结构化版本日志字段 |

### 发布预检与快速验证

| 文件 | 操作 | 职责 |
|---|---|---|
| `scripts/check-version.sh` | 新增 | 校验 RC tag、SDK 基础版本、Release Notes 和干净工作区 |
| `scripts/check-version_test.sh` | 新增 | 在临时 Git 仓库覆盖版本脚本成功和失败路径 |
| `.gitignore` | 修改 | 忽略根 `.pnpm-store/`，不改动缓存内容 |
| `Makefile` | 修改 | 提供 `verify-go-quick`、`verify-web-quick`、`verify-quick` |
| `package.json` | 修改 | 暴露根 `typecheck` 命令 |
| `apps/web/package.json` | 修改 | 暴露 Web `typecheck` 命令 |
| `.github/workflows/ci.yml` | 新增 | PR 与 main push 的两个并行快速 Job |

### 治理与文档

| 文件 | 操作 | 职责 |
|---|---|---|
| `LICENSE` | 新增 | 未修改的 Apache License 2.0 正文 |
| `NOTICE` | 新增 | 项目必要版权与归属通知 |
| `CODE_OF_CONDUCT.md` | 新增 | Contributor Covenant 2.1 与真实私密报告入口 |
| `CONTRIBUTING.md` | 新增 | 环境、分支、TDD、生成物与验证规则 |
| `SECURITY.md` | 新增 | 支持版本、私密漏洞报告与协调披露流程 |
| `.github/ISSUE_TEMPLATE/bug_report.yml` | 新增 | 结构化 Bug 报告 |
| `.github/ISSUE_TEMPLATE/feature_request.yml` | 新增 | 结构化功能建议 |
| `.github/ISSUE_TEMPLATE/config.yml` | 新增 | 禁用空白 Issue 并引导安全报告 |
| `.github/pull_request_template.md` | 新增 | PR 范围、测试、生成物与安全检查清单 |
| `README.md` | 修改 | 增加快速验证、版本命令和社区入口 |
| `docs/sdk/compatibility.md` | 修改 | 区分 SDK/API 版本与应用构建版本 |
| `docs/sdk/quickstart.md` | 修改 | 把 `verify-quick` 加入节点提交前流程 |

---

### 任务 1：Buildinfo 纯函数包

**文件：**
- 新增：`internal/buildinfo/buildinfo.go`
- 新增：`internal/buildinfo/buildinfo_test.go`

**接口：**
- 输入：`agentnode.Version` 与 `agentnode.APIVersion`。
- 输出：`type Info struct { Version, SDKVersion, APIVersion, Revision string; Dirty bool }` 与 `func Current() Info`。
- 内部测试接缝：`func resolve(override string, info *debug.BuildInfo, ok bool) Info`。

- [ ] **步骤 1：编写失败的 buildinfo 测试**

创建 `internal/buildinfo/buildinfo_test.go`：

```go
package buildinfo

import (
	"reflect"
	"runtime/debug"
	"testing"
)

func TestResolveBuildInfo(t *testing.T) {
	tests := []struct {
		name     string
		override string
		info     *debug.BuildInfo
		ok       bool
		want     Info
	}{
		{
			name:     "override wins over module version",
			override: "v0.2.0-rc.9",
			info: &debug.BuildInfo{
				Main: debug.Module{Version: "v0.2.0-rc.1"},
				Settings: []debug.BuildSetting{
					{Key: "vcs.revision", Value: "abc123"},
					{Key: "vcs.modified", Value: "true"},
				},
			},
			ok: true,
			want: Info{
				Version: "v0.2.0-rc.9", SDKVersion: "0.2.0",
				APIVersion: "agent-studio.dev/v1alpha1",
				Revision: "abc123", Dirty: true,
			},
		},
		{
			name: "tagged module version",
			info: &debug.BuildInfo{
				Main: debug.Module{Version: "v0.2.0-rc.1"},
				Settings: []debug.BuildSetting{
					{Key: "vcs.revision", Value: "deadbeef"},
					{Key: "vcs.modified", Value: "false"},
				},
			},
			ok: true,
			want: Info{
				Version: "v0.2.0-rc.1", SDKVersion: "0.2.0",
				APIVersion: "agent-studio.dev/v1alpha1",
				Revision: "deadbeef", Dirty: false,
			},
		},
		{
			name: "development fallback",
			info: &debug.BuildInfo{
				Main: debug.Module{Version: "(devel)"},
				Settings: []debug.BuildSetting{
					{Key: "vcs.modified", Value: "not-a-bool"},
				},
			},
			ok: true,
			want: Info{
				Version: "0.2.0-dev", SDKVersion: "0.2.0",
				APIVersion: "agent-studio.dev/v1alpha1",
				Revision: "unknown", Dirty: false,
			},
		},
		{
			name: "missing build info",
			want: Info{
				Version: "0.2.0-dev", SDKVersion: "0.2.0",
				APIVersion: "agent-studio.dev/v1alpha1",
				Revision: "unknown", Dirty: false,
			},
		},
	}

	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {
			got := resolve(test.override, test.info, test.ok)
			if !reflect.DeepEqual(got, test.want) {
				t.Fatalf("resolve() = %#v, want %#v", got, test.want)
			}
		})
	}
}
```

- [ ] **步骤 2：运行测试并确认红灯**

运行：

```bash
CGO_ENABLED=0 go test ./internal/buildinfo -count=1
```

预期：FAIL，原因是 `Info` 和 `resolve` 尚未定义。

- [ ] **步骤 3：实现最小 buildinfo 包**

创建 `internal/buildinfo/buildinfo.go`：

```go
package buildinfo

import (
	"runtime/debug"
	"strconv"

	"github.com/yyl1212/agent-studio/sdk/go/agentnode"
)

const developmentVersion = "0.2.0-dev"

var versionOverride string

type Info struct {
	Version    string
	SDKVersion string
	APIVersion string
	Revision   string
	Dirty      bool
}

func Current() Info {
	info, ok := debug.ReadBuildInfo()
	return resolve(versionOverride, info, ok)
}

func resolve(override string, info *debug.BuildInfo, ok bool) Info {
	current := Info{
		Version:    developmentVersion,
		SDKVersion: agentnode.Version,
		APIVersion: agentnode.APIVersion,
		Revision:   "unknown",
	}
	if override != "" {
		current.Version = override
	} else if ok && info != nil && info.Main.Version != "" && info.Main.Version != "(devel)" {
		current.Version = info.Main.Version
	}
	if !ok || info == nil {
		return current
	}
	for _, setting := range info.Settings {
		switch setting.Key {
		case "vcs.revision":
			if setting.Value != "" {
				current.Revision = setting.Value
			}
		case "vcs.modified":
			if dirty, err := strconv.ParseBool(setting.Value); err == nil {
				current.Dirty = dirty
			}
		}
	}
	return current
}
```

- [ ] **步骤 4：运行聚焦测试和包级 vet**

运行：

```bash
CGO_ENABLED=0 go test ./internal/buildinfo -count=1
CGO_ENABLED=0 go vet ./internal/buildinfo
```

预期：两个命令均以 0 退出；`TestResolveBuildInfo` 报告 PASS。

- [ ] **步骤 5：提交 buildinfo 包**

```bash
git add internal/buildinfo/buildinfo.go internal/buildinfo/buildinfo_test.go
git commit -m "feat: add unified build information"
```

---

### 任务 2：CLI 版本与 API 启动日志

**文件：**
- 修改：`internal/cli/app.go`
- 修改：`internal/cli/app_test.go`
- 修改：`apps/api/cmd/server/main.go`
- 修改：`apps/api/cmd/server/main_test.go`

**接口：**
- 输入：任务 1 的 `buildinfo.Info` 与 `buildinfo.Current()`。
- 输出 CLI 格式：`agent-studio <buildVersion> (sdk <sdkVersion>; api <apiVersion>; commit <revision>; dirty <true|false>)`。
- 输出服务日志事件 `server starting`，包含 `version`、`sdk_version`、`api_version`、`commit` 与 `dirty`。

- [ ] **步骤 1：添加失败的确定性 CLI 版本测试**

从 `TestRunTopLevelCommands` 删除现有 `version` 用例，并向 `internal/cli/app_test.go` 添加：

```go
func TestRunVersionReportsBuildAndProtocolVersions(t *testing.T) {
	var stdout bytes.Buffer
	var stderr bytes.Buffer
	code := run(context.Background(), []string{"version"}, &stdout, &stderr, appDependencies{
		buildInfo: func() buildinfo.Info {
			return buildinfo.Info{
				Version: "v0.2.0-rc.1", SDKVersion: "0.2.0",
				APIVersion: "agent-studio.dev/v1alpha1",
				Revision: "abc123", Dirty: true,
			}
		},
	})
	want := "agent-studio v0.2.0-rc.1 (sdk 0.2.0; api agent-studio.dev/v1alpha1; commit abc123; dirty true)\n"
	if code != 0 || stdout.String() != want || stderr.Len() != 0 {
		t.Fatalf("code=%d stdout=%q stderr=%q", code, stdout.String(), stderr.String())
	}
}
```

在测试导入中加入 `github.com/yyl1212/agent-studio/internal/buildinfo`。

- [ ] **步骤 2：添加失败的服务日志测试**

为 `apps/api/cmd/server/main_test.go` 增加 `bytes`、`encoding/json`、`log/slog` 与 `internal/buildinfo` 导入，然后添加：

```go
func TestLogBuildInfoIncludesStableFields(t *testing.T) {
	var output bytes.Buffer
	logger := slog.New(slog.NewJSONHandler(&output, nil))
	logBuildInfo(logger, buildinfo.Info{
		Version: "v0.2.0-rc.1", SDKVersion: "0.2.0",
		APIVersion: "agent-studio.dev/v1alpha1",
		Revision: "abc123", Dirty: true,
	})

	var event map[string]any
	if err := json.Unmarshal(output.Bytes(), &event); err != nil {
		t.Fatal(err)
	}
	want := map[string]any{
		"msg": "server starting", "version": "v0.2.0-rc.1",
		"sdk_version": "0.2.0", "api_version": "agent-studio.dev/v1alpha1",
		"commit": "abc123", "dirty": true,
	}
	for key, value := range want {
		if event[key] != value {
			t.Fatalf("event[%q]=%#v, want %#v", key, event[key], value)
		}
	}
}
```

- [ ] **步骤 3：运行聚焦测试并确认红灯**

运行：

```bash
CGO_ENABLED=0 go test ./internal/cli ./apps/api/cmd/server -count=1
```

预期：FAIL，原因是 `appDependencies.buildInfo` 和 `logBuildInfo` 尚不存在。

- [ ] **步骤 4：向 CLI 注入 buildinfo**

在 `internal/cli/app.go` 中：

1. 用 `internal/buildinfo` 替换直接的 `agentnode` 导入。
2. 向 `appDependencies` 添加字段：

```go
buildInfo func() buildinfo.Info
```

3. 在 `Run` 中加入依赖：

```go
buildInfo: buildinfo.Current,
```

4. 将成功的 `version` 输出替换为：

```go
info := dependencies.buildInfo()
_, _ = fmt.Fprintf(
	stdout,
	"agent-studio %s (sdk %s; api %s; commit %s; dirty %t)\n",
	info.Version,
	info.SDKVersion,
	info.APIVersion,
	info.Revision,
	info.Dirty,
)
return 0
```

保留现有的多余参数校验，并确保它发生在调用 `buildInfo` 之前。

- [ ] **步骤 5：添加 API 启动日志辅助函数**

在 `apps/api/cmd/server/main.go` 中导入 `github.com/yyl1212/agent-studio/internal/buildinfo`。

把以下辅助函数调用作为 `run` 的第一条语句：

```go
logBuildInfo(logger, buildinfo.Current())
```

在 `run` 下方添加这个聚焦辅助函数：

```go
func logBuildInfo(logger *slog.Logger, info buildinfo.Info) {
	logger.Info(
		"server starting",
		"version", info.Version,
		"sdk_version", info.SDKVersion,
		"api_version", info.APIVersion,
		"commit", info.Revision,
		"dirty", info.Dirty,
	)
}
```

- [ ] **步骤 6：运行聚焦测试与 CLI 消费者测试**

运行：

```bash
CGO_ENABLED=0 go test ./internal/buildinfo ./internal/cli ./cmd/agent-studio ./apps/api/cmd/server -count=1
```

预期：所有包 PASS；CLI 测试匹配完整稳定输出，服务测试找到全部五个字段。

- [ ] **步骤 7：提交构建身份接入**

```bash
git add internal/cli/app.go internal/cli/app_test.go apps/api/cmd/server/main.go apps/api/cmd/server/main_test.go
git commit -m "feat: report build version in cli and api"
```

---

### 任务 3：RC 版本预检与 pnpm store 忽略规则

**文件：**
- 新增：`scripts/check-version.sh`
- 新增：`scripts/check-version_test.sh`
- 修改：`.gitignore`

**接口：**
- 输入：单个 `v<major>.<minor>.<patch>-rc.<positive integer>` 参数、`sdk/go/agentnode/node.go`、Git 状态与 `docs/releases/<tag>.md`。
- 输出：成功时以 0 退出并打印 `release preflight ok: <tag>`；失败时非零退出并打印一条稳定诊断。

- [ ] **步骤 1：编写失败的 shell 集成测试**

创建 `scripts/check-version_test.sh`：

```sh
#!/bin/sh
set -eu

test_root=$(mktemp -d "${TMPDIR:-/tmp}/agent-studio-version-test.XXXXXX")
trap 'rm -rf "$test_root"' EXIT HUP INT TERM

make_fixture() {
	name=$1
	root="$test_root/$name"
	mkdir -p "$root/scripts" "$root/sdk/go/agentnode" "$root/docs/releases"
	cp "$(dirname "$0")/check-version.sh" "$root/scripts/check-version.sh"
	printf '%s\n' 'package agentnode' 'const Version = "0.2.0"' > "$root/sdk/go/agentnode/node.go"
	printf '%s\n' '# v0.2.0-rc.1' > "$root/docs/releases/v0.2.0-rc.1.md"
	git init -q "$root"
	git -C "$root" config user.name "Agent Studio Test"
	git -C "$root" config user.email "test@example.invalid"
	git -C "$root" config commit.gpgsign false
	git -C "$root" add .
	git -C "$root" commit -qm "fixture"
	printf '%s\n' "$root"
}

expect_failure() {
	expected=$1
	shift
	set +e
	output=$("$@" 2>&1)
	status=$?
	set -e
	if [ "$status" -eq 0 ]; then
		printf 'expected failure containing %s\n' "$expected" >&2
		exit 1
	fi
	case "$output" in
		*"$expected"*) ;;
		*)
			printf 'output %s does not contain %s\n' "$output" "$expected" >&2
			exit 1
			;;
	esac
}

valid=$(make_fixture valid)
valid_output=$(sh "$valid/scripts/check-version.sh" v0.2.0-rc.1)
[ "$valid_output" = "release preflight ok: v0.2.0-rc.1" ]

expect_failure "invalid release tag" sh "$valid/scripts/check-version.sh" 0.2.0

mismatch=$(make_fixture mismatch)
printf '%s\n' 'package agentnode' 'const Version = "0.3.0"' > "$mismatch/sdk/go/agentnode/node.go"
git -C "$mismatch" add .
git -C "$mismatch" commit -qm "change version"
expect_failure "SDK version mismatch" sh "$mismatch/scripts/check-version.sh" v0.2.0-rc.1

missing_notes=$(make_fixture missing-notes)
rm "$missing_notes/docs/releases/v0.2.0-rc.1.md"
git -C "$missing_notes" add -u
git -C "$missing_notes" commit -qm "remove notes"
expect_failure "missing release notes" sh "$missing_notes/scripts/check-version.sh" v0.2.0-rc.1

dirty=$(make_fixture dirty)
printf '\n' >> "$dirty/sdk/go/agentnode/node.go"
expect_failure "worktree is not clean" sh "$dirty/scripts/check-version.sh" v0.2.0-rc.1

untracked=$(make_fixture untracked)
printf '%s\n' "untracked" > "$untracked/local.txt"
expect_failure "worktree is not clean" sh "$untracked/scripts/check-version.sh" v0.2.0-rc.1

ignored=$(make_fixture ignored)
printf '%s\n' '/.pnpm-store/' > "$ignored/.gitignore"
git -C "$ignored" add .gitignore
git -C "$ignored" commit -qm "ignore pnpm store"
mkdir -p "$ignored/.pnpm-store"
printf '%s\n' "cache" > "$ignored/.pnpm-store/cache"
sh "$ignored/scripts/check-version.sh" v0.2.0-rc.1 >/dev/null

printf '%s\n' "check-version tests passed"
```

赋予可执行权限：

```bash
chmod +x scripts/check-version_test.sh
```

- [ ] **步骤 2：运行 shell 测试并确认红灯**

运行：

```bash
sh scripts/check-version_test.sh
```

预期：复制 `scripts/check-version.sh` 时 FAIL，因为该文件尚不存在。

- [ ] **步骤 3：实现发布预检**

创建 `scripts/check-version.sh`：

```sh
#!/bin/sh
set -eu

if [ "$#" -ne 1 ]; then
	printf '%s\n' "usage: scripts/check-version.sh <vX.Y.Z-rc.N>" >&2
	exit 2
fi

tag=$1
if ! printf '%s\n' "$tag" | grep -Eq '^v[0-9]+\.[0-9]+\.[0-9]+-rc\.[1-9][0-9]*$'; then
	printf 'invalid release tag: %s\n' "$tag" >&2
	exit 1
fi

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
repo_root=$(git -C "$script_dir/.." rev-parse --show-toplevel)
cd "$repo_root"

base_version=${tag#v}
base_version=${base_version%-rc.*}
sdk_version=$(sed -n 's/^const Version = "\([^"]*\)"$/\1/p' sdk/go/agentnode/node.go)
if [ -z "$sdk_version" ] || [ "$sdk_version" != "$base_version" ]; then
	printf 'SDK version mismatch: tag=%s sdk=%s\n' "$base_version" "${sdk_version:-missing}" >&2
	exit 1
fi

release_notes="docs/releases/$tag.md"
if [ ! -s "$release_notes" ]; then
	printf 'missing release notes: %s\n' "$release_notes" >&2
	exit 1
fi

status=$(git status --porcelain --untracked-files=normal)
if [ -n "$status" ]; then
	printf '%s\n' "worktree is not clean" >&2
	exit 1
fi

printf 'release preflight ok: %s\n' "$tag"
```

赋予可执行权限，并把以下根目录规则准确追加到 `.gitignore`：

```text
/.pnpm-store/
```

运行：

```bash
chmod +x scripts/check-version.sh
```

- [ ] **步骤 4：运行全部脚本用例并验证缓存忽略行为**

运行：

```bash
sh scripts/check-version_test.sh
git check-ignore -v .pnpm-store
```

预期：

- 测试脚本打印 `check-version tests passed` 并以 0 退出。
- `git check-ignore` identifies the new `/.pnpm-store/` rule.
- `.pnpm-store/` 下没有文件被暂存。

- [ ] **步骤 5：提交预检**

```bash
git add .gitignore scripts/check-version.sh scripts/check-version_test.sh
git commit -m "build: add release version preflight"
```

---

### 任务 4：本地快速验证目标

**文件：**
- 修改：`Makefile`
- 修改：`package.json`
- 修改：`apps/web/package.json`

**接口：**
- 输出：`make verify-go-quick`、`make verify-web-quick` 与聚合目标 `make verify-quick`。
- `verify-go-quick` performs generation drift, all no-DB Go unit tests, vet, and version-script tests.
- `verify-web-quick` performs OpenAPI drift, lint, explicit typecheck, unit tests, and production build.

- [ ] **步骤 1：确认目标尚不存在**

运行：

```bash
make verify-go-quick
```

预期：以 `No rule to make target 'verify-go-quick'` FAIL。

- [ ] **步骤 2：添加显式 typecheck 脚本**

向 `package.json` 添加根脚本：

```json
"typecheck": "corepack pnpm@10.34.5 --filter @agent-studio/web typecheck"
```

向 `apps/web/package.json` 添加脚本：

```json
"typecheck": "tsc --noEmit"
```

为兼容性保留现有 `lint` 脚本；虽然当前 lint 命令同样基于 TypeScript，阶段 A 仍明确运行两个命令名。

- [ ] **步骤 3：添加 Make 目标**

向 `.PHONY` 声明加入 `verify-go-quick verify-web-quick verify-quick`，并添加：

```make
verify-go-quick: check-generated
	CGO_ENABLED=0 go test ./... -count=1
	CGO_ENABLED=0 go vet ./...
	sh scripts/check-version_test.sh

verify-web-quick:
	corepack pnpm@10.34.5 --filter @agent-studio/web generate:api
	git diff --exit-code -- apps/web/src/lib/api/generated.ts
	corepack pnpm@10.34.5 lint
	corepack pnpm@10.34.5 typecheck
	corepack pnpm@10.34.5 test
	corepack pnpm@10.34.5 build

verify-quick: verify-go-quick verify-web-quick
```

本任务不让现有 `verify` 依赖 `verify-quick`；其 PostgreSQL 回归语义保持不变。

- [ ] **步骤 4：分别运行两部分**

运行：

```bash
make verify-go-quick
make verify-web-quick
```

预期：两个命令均以 0 退出。Go 部分不得调用 Docker；Web 部分不得改变 `apps/web/src/lib/api/generated.ts`。

- [ ] **步骤 5：运行聚合命令**

运行：

```bash
make verify-quick
git diff --exit-code -- apps/api/internal/generated/nodes_gen.go apps/web/src/lib/api/generated.ts
```

预期：两个命令均以 0 退出，生成文件没有差异。

- [ ] **步骤 6：提交快速验证**

```bash
git add Makefile package.json apps/web/package.json
git commit -m "build: add quick verification targets"
```

---

### 任务 5：GitHub Actions 快速 CI

**文件：**
- 新增：`.github/workflows/ci.yml`

**接口：**
- 输入：任务 4 的 `make verify-go-quick` 与 `make verify-web-quick`。
- 输出：面向合入 `main` 的 PR 和推送到 `main` 的只读 `go`、`web` Job。

- [ ] **步骤 1：确认工作流尚不存在**

运行：

```bash
test -f .github/workflows/ci.yml
```

预期：以 1 退出。

- [ ] **步骤 2：添加工作流**

创建 `.github/workflows/ci.yml`：

```yaml
name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  go:
    name: Go quick verification
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CGO_ENABLED: "0"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Verify Go
        run: make verify-go-quick

  web:
    name: Web quick verification
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5
          run_install: false
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Enable Corepack
        run: corepack enable
      - name: Install dependencies
        run: corepack pnpm@10.34.5 install --frozen-lockfile
      - name: Verify Web
        run: make verify-web-quick
```

- [ ] **步骤 3：校验 YAML 与固定安全属性**

运行：

```bash
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); puts "workflow yaml ok"'
rg -n 'contents: read|pull_request:|branches:|make verify-go-quick|make verify-web-quick|node-version: "24"|version: 10.34.5' .github/workflows/ci.yml
rg -n 'pull_request_target|docker|playwright|TEST_DATABASE_URL|permissions: write' .github/workflows/ci.yml
```

预期：

- Ruby 打印 `workflow yaml ok`。
- 正向搜索找到全部必需设置。
- 最后的负向搜索无输出并以 1 退出。

- [ ] **步骤 4：重新运行与 Job 完全一致的本地命令**

运行：

```bash
make verify-go-quick
make verify-web-quick
```

预期：两者均以 0 退出。

- [ ] **步骤 5：提交 CI**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: add quick pull request verification"
```

---

### 任务 6：许可证、NOTICE 与行为准则

**文件：**
- 新增：`LICENSE`
- 新增：`NOTICE`
- 新增：`CODE_OF_CONDUCT.md`

**接口：**
- 输出：Apache-2.0 授权、最小 NOTICE 归属与 Contributor Covenant 2.1 执行政策。
- 私密行为准则报告渠道是 `https://github.com/yyl1212/agent-studio/security/advisories/new`；只有任务 7 启用仓库私密漏洞报告后，该渠道才可用。

- [ ] **步骤 1：确认治理文件尚不存在**

运行：

```bash
test -e LICENSE
test -e NOTICE
test -e CODE_OF_CONDUCT.md
```

预期：三个文件都不存在；第一个命令以 1 退出。

- [ ] **步骤 2：添加标准 Apache-2.0 许可证**

使用 `apply_patch` 创建 `LICENSE`，内容采用以下来源中标题为 `Apache License`、`Version 2.0, January 2004` 的完整未修改正文：

```text
https://www.apache.org/licenses/LICENSE-2.0.txt
```

文件必须包含标准 `END OF TERMS AND CONDITIONS` 附录；许可证正文内部不得添加项目专属版权行。

- [ ] **步骤 3：添加确定的 NOTICE**

创建 `NOTICE`：

```text
Agent Studio
Copyright 2026 Agent Studio contributors

This product includes software developed by the Agent Studio contributors.
```

- [ ] **步骤 4：添加 Contributor Covenant 2.1**

使用 `apply_patch`，基于官方简体中文 Contributor Covenant 2.1 完整正文创建 `CODE_OF_CONDUCT.md`：

```text
https://www.contributor-covenant.org/zh-cn/version/2/1/code_of_conduct/
```

使用标题 `# 贡献者公约`。把标准 `## 监督` 章节中的联系信息占位内容替换为以下项目段落：

```markdown
辱骂、骚扰或其他不可接受的行为，可通过仓库的
[私密报告入口](https://github.com/yyl1212/agent-studio/security/advisories/new)
向负责监督的社区领袖报告。请在标题前添加 `[CODE OF CONDUCT]`。
报告仅对仓库维护者可见；不要创建包含个人信息或敏感信息的公开 Issue。
```

保留标准 `## 参见` 章节及其 2.1 版本链接。

- [ ] **步骤 5：验证法律标记与联系渠道**

运行：

```bash
rg -n '^Apache License$|Version 2.0, January 2004|END OF TERMS AND CONDITIONS' LICENSE
rg -n '^Agent Studio$|Copyright 2026 Agent Studio contributors' NOTICE
rg -n '^# 贡献者公约$|私密报告入口|\[CODE OF CONDUCT\]|zh-cn/version/2/1' CODE_OF_CONDUCT.md
rg -n 'I[N]SERT|T[O]DO|T[B]D|插[入]联系方式|e[x]ample\.com|y[o]ur\.email' LICENSE NOTICE CODE_OF_CONDUCT.md
git diff --check
```

预期：必需标记均存在，占位内容搜索无输出并以 1 退出，`git diff --check` 以 0 退出。

- [ ] **步骤 6：提交法律与行为准则文件**

```bash
git add LICENSE NOTICE CODE_OF_CONDUCT.md
git commit -m "docs: add license and code of conduct"
```

---

### 任务 7：贡献政策、安全政策与 GitHub 模板

**文件：**
- 新增：`CONTRIBUTING.md`
- 新增：`SECURITY.md`
- 新增：`.github/ISSUE_TEMPLATE/bug_report.yml`
- 新增：`.github/ISSUE_TEMPLATE/feature_request.yml`
- 新增：`.github/ISSUE_TEMPLATE/config.yml`
- 新增：`.github/pull_request_template.md`

**接口：**
- 输出：贡献者流程、支持版本政策、结构化 Issue 入口、PR 检查清单与可用的私密漏洞渠道。

- [ ] **步骤 1：添加贡献指南**

创建 `CONTRIBUTING.md`：

````markdown
# 贡献指南

感谢你改进 Agent Studio。提交代码表示你同意贡献内容按仓库的
[Apache License 2.0](LICENSE) 发布，并遵守
[行为准则](CODE_OF_CONDUCT.md)。

## 环境

- Go 1.26，项目 toolchain 为 1.26.5
- Node.js 24
- Corepack 与 pnpm 10.34.5
- 完整回归需要 Docker Desktop 或 Docker Compose

安装依赖：

```bash
corepack enable
corepack pnpm@10.34.5 install --frozen-lockfile
```

## 开发流程

1. 从最新的 `main` 创建 `codex/<简短主题>` 分支。
2. 先编写能稳定复现需求或缺陷的失败测试。
3. 编写使测试通过的最小实现，并保持提交范围单一。
4. 运行 `make verify-quick`。
5. 涉及数据库或发布门禁时再运行 `make verify`。
6. 在 Pull Request 中说明行为变化、测试证据和安全影响。

所有 Go 调试、测试和生成命令必须设置 `CGO_ENABLED=0`。

## 生成文件

不要直接编辑以下文件：

- `apps/api/internal/generated/nodes_gen.go`
- `apps/web/src/lib/api/generated.ts`

节点 manifest 变化后运行：

```bash
CGO_ENABLED=0 go run ./cmd/agent-studio generate
```

OpenAPI 变化后运行：

```bash
corepack pnpm@10.34.5 --filter @agent-studio/web generate:api
```

## 验证

不依赖 Docker 的提交前检查：

```bash
make verify-quick
```

需要 Docker PostgreSQL 的完整检查：

```bash
make verify
make test-e2e
make test-sdk-e2e
```

## 安全问题

不要在公开 Issue、Pull Request、日志或截图中提交密钥和未披露漏洞。
安全问题请按 [安全策略](SECURITY.md) 私密报告。
````

- [ ] **步骤 2：添加安全政策**

创建 `SECURITY.md`：

```markdown
# 安全策略

## 支持版本

| 版本 | 状态 |
|---|---|
| 0.2.x | 开发者预览，接受安全修复 |
| 0.1.x 及更早 | 不再支持 |

0.2.x 尚未提供 v1 级兼容承诺。安全修复可能要求升级到新的 RC 或
0.2 patch 版本。

## 私密报告漏洞

请使用 GitHub 的
[私密漏洞报告入口](https://github.com/yyl1212/agent-studio/security/advisories/new)。
不要为未披露漏洞创建公开 Issue。

报告请包含：

- 受影响的提交或版本
- 最小复现步骤
- 实际影响与可利用条件
- 已知缓解措施
- 希望使用的署名

请删除请求中的真实密钥、个人数据和无关日志。维护者会通过同一个
Security Advisory 协调验证、修复和披露。开发者预览阶段不承诺固定响应
时限；在修复可用前，请不要公开利用细节。
```

- [ ] **步骤 3：添加 Bug Issue 表单**

创建 `.github/ISSUE_TEMPLATE/bug_report.yml`：

```yaml
name: Bug 报告
description: 报告可稳定复现的 Agent Studio 缺陷
title: "[Bug]: "
labels: []
body:
  - type: markdown
    attributes:
      value: |
        感谢反馈。安全漏洞请使用私密报告入口，不要在这里粘贴密钥。
  - type: checkboxes
    id: checks
    attributes:
      label: 提交前检查
      options:
        - label: 我已搜索现有 Issue，未发现相同问题
          required: true
        - label: 此报告不包含密钥、个人数据或未披露漏洞
          required: true
  - type: input
    id: version
    attributes:
      label: 版本或提交
      description: 请填写 agent-studio version 输出或 Git commit
    validations:
      required: true
  - type: dropdown
    id: environment
    attributes:
      label: 运行环境
      options:
        - macOS
        - Linux
        - Windows + Docker Desktop
        - 其他
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: 复现步骤
      description: 提供最小、确定的操作序列
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: 期望行为
    validations:
      required: true
  - type: textarea
    id: actual
    attributes:
      label: 实际行为
    validations:
      required: true
  - type: textarea
    id: logs
    attributes:
      label: 已脱敏日志
      description: 仅粘贴复现所需片段，并先删除 URL、Token 和个人数据
```

- [ ] **步骤 4：添加功能建议表单与模板配置**

创建 `.github/ISSUE_TEMPLATE/feature_request.yml`：

```yaml
name: 功能建议
description: 描述用户问题和最小可行改进
title: "[Feature]: "
labels: []
body:
  - type: checkboxes
    id: checks
    attributes:
      label: 提交前检查
      options:
        - label: 我已搜索现有 Issue 和路线文档
          required: true
  - type: textarea
    id: problem
    attributes:
      label: 用户问题
      description: 描述谁在什么场景下遇到了什么阻碍
    validations:
      required: true
  - type: textarea
    id: proposal
    attributes:
      label: 最小方案
      description: 描述希望看到的行为，不要求预先设计内部实现
    validations:
      required: true
  - type: textarea
    id: alternatives
    attributes:
      label: 已考虑的替代方案
  - type: textarea
    id: scope
    attributes:
      label: 范围与兼容性
      description: 说明是否影响 SDK、工作流格式、数据库或安全边界
```

创建 `.github/ISSUE_TEMPLATE/config.yml`：

```yaml
blank_issues_enabled: false
contact_links:
  - name: 私密报告安全漏洞
    url: https://github.com/yyl1212/agent-studio/security/advisories/new
    about: 不要在公开 Issue 中披露漏洞或密钥
```

- [ ] **步骤 5：添加 Pull Request 模板**

创建 `.github/pull_request_template.md`：

```markdown
## 变更

- 解决的问题：
- 方案与边界：

## 验证

- [ ] 新行为先有失败测试或明确的配置验证基线
- [ ] `make verify-quick` 通过
- [ ] 相关数据库、SDK 或 Playwright 回归已运行
- [ ] 生成文件由生成器更新且无漂移

## 兼容与安全

- [ ] 未提交密钥、个人数据或未脱敏日志
- [ ] 已说明 SDK、工作流、数据库和环境变量影响
- [ ] 文档已随行为变化更新
```

- [ ] **步骤 6：验证 Markdown 与 GitHub YAML**

运行：

```bash
ruby -e 'require "yaml"; Dir[".github/ISSUE_TEMPLATE/*.yml"].each { |path| YAML.load_file(path) }; puts "issue yaml ok"'
rg -n 'make verify-quick|CGO_ENABLED=0|生成文件|SECURITY.md' CONTRIBUTING.md
rg -n '0.2.x|security/advisories/new|不要.*公开 Issue' SECURITY.md
rg -n 'security/advisories/new|blank_issues_enabled: false' .github/ISSUE_TEMPLATE/config.yml
rg -n 'T[O]DO|T[B]D|I[N]SERT|插[入]联系方式|e[x]ample\.com|y[o]ur\.email' CONTRIBUTING.md SECURITY.md .github CODE_OF_CONDUCT.md
git diff --check
```

预期：YAML 解析与正向搜索成功；占位内容搜索以 1 退出；差异检查以 0 退出。

- [ ] **步骤 7：提交贡献与安全文件**

```bash
git add CONTRIBUTING.md SECURITY.md .github/ISSUE_TEMPLATE .github/pull_request_template.md
git commit -m "docs: add contribution and security governance"
```

- [ ] **步骤 8：取得授权并启用私密漏洞报告**

先读取当前状态：

```bash
gh api repos/yyl1212/agent-studio/private-vulnerability-reporting --jq .enabled
```

变更前预期：`false`。

暂停并取得用户对仓库设置变更的明确授权。授权后运行：

```bash
gh api --method PUT repos/yyl1212/agent-studio/private-vulnerability-reporting
gh api repos/yyl1212/agent-studio/private-vulnerability-reporting --jq .enabled
```

预期：最后一个命令打印 `true`。若权限不足或用户拒绝，记录为阶段阻塞项；不得用公开 Issue 替代私密链接。

---

### 任务 8：面向开发者的发布基础文档

**文件：**
- 修改：`README.md`
- 修改：`docs/sdk/compatibility.md`
- 修改：`docs/sdk/quickstart.md`

**接口：**
- 输入：任务 2 的版本输出、任务 4 的快速目标，以及任务 6–7 的治理路径。
- 输出：一条可发现的本地工作流，但不声称 `v0.2.0-rc.1` 已经发布。

- [ ] **步骤 1：记录缺失文档断言**

运行：

```bash
rg -n 'make verify-quick|agent-studio .*dirty|CONTRIBUTING.md|SECURITY.md' README.md
rg -n '应用构建版本|0.2.0-dev|v0.2.0-rc.1' docs/sdk/compatibility.md
```

预期：由于新说明尚不存在，至少一个搜索以 1 退出。

- [ ] **步骤 2：更新 README 验证与社区章节**

在 `README.md` 中：

1. 在 SDK CLI 命令列表后加入 `version` 示例：

````markdown
查看当前应用构建、SDK/API 契约和提交信息：

```bash
CGO_ENABLED=0 go run ./cmd/agent-studio version
```

源码开发构建显示 `0.2.0-dev`；从版本标签安装时显示对应 tag。
````

2. Replace the beginning of `## 验证` with:

````markdown
## 验证

不启动 Docker 的提交前快速检查：

```bash
make verify-quick
```

该命令运行 Go 生成物检查、单元测试、vet、发布脚本测试，以及前端
OpenAPI 生成物检查、lint、类型检查、组件测试和生产构建。

需要 Docker PostgreSQL 的完整回归：

```bash
make verify
make test-e2e
make test-sdk-e2e
```
````

保留该区块之后对三个完整命令的现有解释。

3. 在文末加入 `## 社区与安全` 章节：

```markdown
## 社区与安全

- 贡献流程：[CONTRIBUTING.md](CONTRIBUTING.md)
- 行为准则：[CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)
- 安全报告：[SECURITY.md](SECURITY.md)
- 许可证：[Apache-2.0](LICENSE)
```

- [ ] **步骤 3：说明 SDK 版本与构建版本的区别**

在 `docs/sdk/compatibility.md` 的 `## 协议与 SDK 版本` 后添加：

```markdown
### 应用构建版本

SDK 版本与应用构建版本独立：

- `agentnode.Version = 0.2.0` 表示公开 Go SDK 契约。
- `agentnode.APIVersion = agent-studio.dev/v1alpha1` 表示节点协议。
- 本地源码构建显示 `0.2.0-dev`。
- 从 tag 安装的 CLI 显示对应模块版本，例如 `v0.2.0-rc.1`。

应用构建版本变化不自动改变 SDK 或节点协议；只有公开契约变化时才按
本文件的兼容规则提升 SDK/API 版本。
```

- [ ] **步骤 4：在 SDK 快速开始中加入快速验证**

在 `docs/sdk/quickstart.md` 中，把 `## 4. 提交前验证` 命令区块改为：

```bash
make check-generated
make verify-quick
make verify
make test-sdk-e2e
```

在其下方添加一句：

```markdown
`verify-quick` 不启动 Docker，适合每次提交前运行；`verify` 和
`test-sdk-e2e` 使用 Docker PostgreSQL，作为合并前完整回归。
```

- [ ] **步骤 5：验证链接、陈述和格式**

运行：

```bash
test -f CONTRIBUTING.md
test -f CODE_OF_CONDUCT.md
test -f SECURITY.md
test -f LICENSE
rg -n 'make verify-quick|0.2.0-dev|CONTRIBUTING.md|SECURITY.md' README.md
rg -n '应用构建版本|0.2.0-dev|v0.2.0-rc.1' docs/sdk/compatibility.md
rg -n 'make verify-quick|不启动 Docker' docs/sdk/quickstart.md
git diff --check
```

预期：所有文件与短语都存在；差异检查以 0 退出。

- [ ] **步骤 6：提交开发者文档**

```bash
git add README.md docs/sdk/compatibility.md docs/sdk/quickstart.md
git commit -m "docs: document quick verification and build versions"
```

---

### 任务 9：阶段 A 审查与回归门禁

**文件：**
- 只读审查：本计划列出的全部文件
- 不允许标签、GitHub Release、Retriever、Webhook、二进制或镜像变更

**接口：**
- 输入：任务 1–8 的全部产物。
- 输出：已经审查并完成本地验证、可选择发布 PR 的阶段 A 分支。

- [ ] **步骤 1：检查完整分支范围**

运行：

```bash
git status --short --branch
git diff --stat origin/main...HEAD
git diff --check origin/main...HEAD
git log --oneline origin/main..HEAD
```

预期：

- 只出现已确认范围内的文件。
- 差异包含已确认的设计规格、本实施计划，以及本计划文件地图列出的实施文件。
- `.pnpm-store/` 因已被忽略而不出现在状态中。
- 差异检查以 0 退出。
- 不存在发布标签或额外生成制品。

- [ ] **步骤 2：运行无 Docker 验收门禁**

运行：

```bash
make verify-quick
```

预期：以 0 退出；Go 测试/vet、脚本测试、Web 测试/typecheck/build 与两项漂移检查全部通过。

- [ ] **步骤 3：运行相关 Docker 回归**

运行：

```bash
make verify
make test-sdk-e2e
```

预期：

- Docker PostgreSQL 达到健康状态。
- 所有 Go 测试与 vet 在 `CGO_ENABLED=0` 下通过。
- 前端 lint、测试与构建通过。
- SDK CLI 黄金路径与通用 Echo 画布测试通过。

- [ ] **步骤 4：验证治理文件与仓库设置**

运行：

```bash
sh scripts/check-version_test.sh
gh api repos/yyl1212/agent-studio/private-vulnerability-reporting --jq .enabled
rg -n 'T[O]DO|T[B]D|I[N]SERT|插[入]联系方式|e[x]ample\.com|y[o]ur\.email' LICENSE NOTICE CONTRIBUTING.md SECURITY.md CODE_OF_CONDUCT.md .github
git status --short
```

预期：

- 脚本测试通过。
- GitHub 打印 `true`。
- 占位内容搜索无输出并以 1 退出。
- Git 状态为空。

- [ ] **步骤 5：请求代码审查**

针对 `origin/main...HEAD` 调用 `superpowers:requesting-code-review`。审查者必须明确检查：

- buildinfo 解析与确定性测试接缝；
- CLI/API 使用同一构建身份；
- shell 引号、临时目录清理、标签校验与脏工作区处理；
- GitHub Actions 权限以及没有 Docker/secrets；
- 治理链接与私密报告可用性；
- 没有阶段 B/C 范围泄漏。

记录全部 P0、P1、P2 发现。对每个可操作问题，在适用时先补失败回归再修复，然后重复步骤 1–4。门禁要求剩余 P0/P1/P2 为零。

- [ ] **步骤 6：在不发布的前提下准备交接**

运行：

```bash
git status --short --branch
git log -1 --oneline
```

预期：分支干净，最新提交属于阶段 A。

报告：

- 已执行命令与准确通过数量；
- 审查结果与有意延后的次要观察；
- Private Vulnerability Reporting 状态；
- 分支与提交 SHA；
- 未创建 push、PR、合并、标签、Release、二进制或镜像。

询问用户是否把分支发布为 Draft PR。获得授权后使用仓库发布流程，并等待两个 GitHub Actions Job 均通过后再请求合并。

---

## 阶段 A 完成定义

- [ ] `internal/buildinfo` 是应用构建身份的唯一来源。
- [ ] CLI 与 API 记录稳定的 version、SDK、API、revision 和 dirty 字段。
- [ ] `scripts/check-version.sh` 的 fixture 成功与失败测试通过。
- [ ] `/.pnpm-store/` 被忽略，用户缓存内容未被修改。
- [ ] `make verify-go-quick`、`make verify-web-quick` 与 `make verify-quick` 通过。
- [ ] GitHub Actions 具备只读、并行 Go/Web 快速 Job，且不含 Docker、Playwright 或安全扫描。
- [ ] Apache-2.0、NOTICE、Contributor Covenant、贡献/安全政策与模板齐全且无占位内容。
- [ ] Private Vulnerability Reporting 已启用并验证。
- [ ] README 与 SDK 文档明确区分应用构建版本和 SDK/API 版本。
- [ ] `make verify` 与 `make test-sdk-e2e` 通过。
- [ ] 代码审查剩余 P0/P1/P2 为零。
- [ ] 分支干净，未发生阶段 B/C 或发布动作。

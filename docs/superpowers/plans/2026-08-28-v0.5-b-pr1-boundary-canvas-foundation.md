# v0.5-B PR1 不变量与画布骨架 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让所有新建与已保存草稿都保持唯一开始/结束节点，并为历史异常草稿提供显式、可恢复的修复流程和空画布引导。

**Architecture:** Go `SaveDraft` 在写库前执行独立的边界数量校验，不调用完整 compiler；Web 用纯函数统一完成边界诊断、删除过滤和修复计划。异常修复采用非乐观保存，画布状态只在 `SaveQueue.flush()` 成功后替换；正常画布编辑继续沿用 `StudioPage`、React Flow 和现有保存队列。

**Tech Stack:** Go 1.26、React 19、TypeScript、React Flow 12.11.2、Vitest 4.1.10、Playwright、Docker PostgreSQL。

**Spec:** `docs/superpowers/specs/2026-08-28-v0.5-b-canvas-ux-design.md`

## Global Constraints

- 以已确认并提交的 `codex/v0.5-b-canvas-ux-design` 为起点，通过 `superpowers:using-git-worktrees` 创建隔离工作树和 `codex/v0.5-b-boundary-canvas-foundation` 分支；进入新工作树后先 `git fetch origin` 并合入最新 `origin/main`，再运行基线验证和修改业务代码。
- 所有功能按 TDD 顺序实施；不得先写实现再补测试。
- Go 调试和测试命令必须设置 `CGO_ENABLED=0`。
- PostgreSQL 集成环境使用项目 Docker PostgreSQL，不切换为本地裸 PostgreSQL。
- 不修改 OpenAPI、数据库迁移、工作流 JSON、节点定义、节点包清单、SDK 或本地 RAG。
- 草稿保存只校验 start/end 数量，不得调用完整 compiler 阻止半成品草稿保存。
- 开始/结束节点可移动、可配置，但普通 UI 操作不可删除、剪切、复制或重复创建。
- 历史异常草稿不得静默修复；重复边界必须选择保留项并明确确认关联边删除。
- 自制视觉素材；不引入新全局状态库、拖拽依赖或外部图片。

---

## 文件结构

### 新增

- `apps/api/internal/workflow/boundaries.go`：后端边界数量校验。
- `apps/api/internal/workflow/boundaries_test.go`：后端合法、缺失、重复与组合问题测试。
- `apps/web/src/features/studio/workflowBoundaries.ts`：边界判定、诊断、删除过滤和 repair plan。
- `apps/web/src/features/studio/workflowBoundaries.test.ts`：上述纯逻辑回归。
- `apps/web/src/features/studio/BoundaryRepairBanner.tsx`：异常横幅、保留项选择、预览与确认。
- `apps/web/src/features/studio/BoundaryRepairBanner.test.tsx`：修复交互和可访问性测试。
- `apps/web/src/features/studio/CanvasEmptyGuide.tsx`：非持久化的首节点引导。
- `apps/web/src/features/studio/CanvasEmptyGuide.test.tsx`：引导显示与触发测试。

### 修改

- `apps/api/internal/workflow/service.go:207-223`：`SaveDraft` 写库前调用轻量校验。
- `apps/api/internal/workflow/service_test.go:300-330`：创建默认边界和拒绝非法保存。
- `apps/api/internal/httpapi/router_test.go:830-850`：验证 422 `WORKFLOW_INVALID` issues 契约。
- `apps/web/src/features/studio/nodePlacement.ts:1-55`：安全边界位置与边界中点。
- `apps/web/src/features/studio/nodePlacement.test.ts`：确定性坐标测试。
- `apps/web/src/features/studio/WorkflowCanvas.tsx:18-145`：只读状态、边界删除入口和 `ViewportPortal` 引导。
- `apps/web/src/features/studio/WorkflowCanvas.test.tsx`：边界删除、只读与引导回归。
- `apps/web/src/features/studio/StudioPage.tsx:42-605`：诊断、保护、非乐观修复、禁用和提示编排。
- `apps/web/src/features/studio/StudioPage.test.tsx:220-330`：页面级删除保护、修复与单次保存测试。
- `apps/web/src/features/studio/StudioCommandBar.tsx:7-66`：持续修复状态和主操作禁用。
- `apps/web/src/features/studio/StudioQuickTools.tsx:5-34`：异常状态下禁用添加。
- `apps/web/src/features/studio/studio.css:1-101,193-228`：紧凑命令条、修复横幅、空图引导和响应式。
- `apps/web/e2e/agent-studio.spec.ts:1-130`：新建、保护、修复和窄屏主链路。

## Task 1: 后端草稿边界不变量

**Files:**
- Create: `apps/api/internal/workflow/boundaries.go`
- Create: `apps/api/internal/workflow/boundaries_test.go`
- Modify: `apps/api/internal/workflow/service.go:207-223`
- Modify: `apps/api/internal/workflow/service_test.go:300-330`

**Interfaces:**
- Consumes: `domain.Graph`、`domain.ValidationIssue`、现有 `ValidationError`。
- Produces: `validateDraftBoundaries(graph domain.Graph) []domain.ValidationIssue`；issue 顺序固定为 start 后 end，`Path` 固定为 `nodes`。

- [ ] **Step 0: 创建隔离工作树并验证最新基线**

从仓库主工作树执行：

```bash
git fetch origin
git worktree add .worktrees/v0.5-b-boundary-canvas-foundation -b codex/v0.5-b-boundary-canvas-foundation codex/v0.5-b-canvas-ux-design
```

进入新工作树后执行：

```bash
git merge --no-edit origin/main
CGO_ENABLED=0 make verify-quick
```

Expected: merge 无冲突，基线 Go 测试/vet、前端测试和生产构建全部 PASS；若 `origin/main` 没有新提交，merge 明确报告 already up to date。

- [ ] **Step 1: 写边界数量与新建默认图失败测试**

```go
func TestValidateDraftBoundaries(t *testing.T) {
	tests := []struct {
		name string
		types []string
		want  []string
	}{
		{name: "valid", types: []string{"start", "template", "end"}},
		{name: "missing both", types: []string{"template"}, want: []string{"WORKFLOW_START_COUNT", "WORKFLOW_END_COUNT"}},
		{name: "duplicate start", types: []string{"start", "start", "end"}, want: []string{"WORKFLOW_START_COUNT"}},
		{name: "duplicate end", types: []string{"start", "end", "end"}, want: []string{"WORKFLOW_END_COUNT"}},
	}
	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {
			graph := domain.Graph{SchemaVersion: 1}
			for index, nodeType := range test.types {
				graph.Nodes = append(graph.Nodes, domain.Node{ID: fmt.Sprintf("node-%d", index), Type: nodeType})
			}
			issues := validateDraftBoundaries(graph)
			got := make([]string, 0, len(issues))
			for _, issue := range issues {
				got = append(got, issue.Code)
				if issue.Path != "nodes" || issue.NodeID != "" || issue.EdgeID != "" { t.Fatalf("issue=%+v", issue) }
			}
			if !slices.Equal(got, test.want) { t.Fatalf("codes=%v want=%v", got, test.want) }
		})
	}
}
```

在 `service_test.go` 增加 `TestCreateIncludesUnconnectedBoundaryNodes`，反序列化 `created.DraftGraph` 并断言 start/end 各一个、`len(graph.Edges) == 0`、坐标为 `(120,180)` 与 `(520,180)`。

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
CGO_ENABLED=0 go test ./apps/api/internal/workflow -run 'TestValidateDraftBoundaries|TestCreateIncludesUnconnectedBoundaryNodes' -count=1
```

Expected: FAIL，`validateDraftBoundaries` 尚未定义。

- [ ] **Step 3: 实现最小边界校验并接入 SaveDraft**

```go
func validateDraftBoundaries(graph domain.Graph) []domain.ValidationIssue {
	startCount, endCount := 0, 0
	for _, node := range graph.Nodes {
		switch node.Type {
		case "start": startCount++
		case "end": endCount++
		}
	}
	issues := make([]domain.ValidationIssue, 0, 2)
	if startCount != 1 {
		issues = append(issues, domain.ValidationIssue{Code: "WORKFLOW_START_COUNT", Message: "工作流必须恰有一个开始节点", Path: "nodes"})
	}
	if endCount != 1 {
		issues = append(issues, domain.ValidationIssue{Code: "WORKFLOW_END_COUNT", Message: "工作流必须恰有一个结束节点", Path: "nodes"})
	}
	return issues
}
```

在 `SaveDraft` 的 `SchemaVersion` 校验之后、`json.Marshal` 之前加入：

```go
if issues := validateDraftBoundaries(graph); len(issues) > 0 {
	return domain.Workflow{}, &ValidationError{Issues: issues}
}
```

- [ ] **Step 4: 增加 SaveDraft 不写库回归并运行**

为 `TestSaveDraftBoundaryGuard` 建立五个子用例：缺失 start 期望 `WORKFLOW_START_COUNT`、缺失 end 期望 `WORKFLOW_END_COUNT`、重复 start 期望 `WORKFLOW_START_COUNT`、重复 end 期望 `WORKFLOW_END_COUNT`，四个拒绝用例都断言 `errors.As(err, &validation)` 且 `store.updateDraftCalls == 0`；第五个用例传入唯一但未连线的 start/end，断言保存成功且 `store.updateDraftCalls == 1`，证明没有调用完整 compiler。

Run:

```bash
CGO_ENABLED=0 go test ./apps/api/internal/workflow -run 'TestValidateDraftBoundaries|TestCreateIncludesUnconnectedBoundaryNodes|TestSaveDraft' -count=1
```

Expected: PASS。

- [ ] **Step 5: 提交后端不变量**

```bash
git add apps/api/internal/workflow/boundaries.go apps/api/internal/workflow/boundaries_test.go apps/api/internal/workflow/service.go apps/api/internal/workflow/service_test.go
git commit -m "feat: enforce workflow boundary invariants"
```

## Task 2: HTTP 422 错误契约回归

**Files:**
- Modify: `apps/api/internal/httpapi/router_test.go:830-850`

**Interfaces:**
- Consumes: `workflow.ValidationError`、`ErrorResponse` 和现有 `writeError`。
- Produces: 为现有 `fixtureWorkflowService` 新增 `saveDraftErr error` 注入点；不新增 API；锁定 HTTP 422、code `WORKFLOW_INVALID`、中文 message 和完整 issues。

- [ ] **Step 1: 写精确响应测试**

```go
func TestWorkflowValidationErrorReturnsIssues(t *testing.T) {
	issues := []domain.ValidationIssue{
		{Code: "WORKFLOW_START_COUNT", Message: "工作流必须恰有一个开始节点", Path: "nodes"},
		{Code: "WORKFLOW_END_COUNT", Message: "工作流必须恰有一个结束节点", Path: "nodes"},
	}
	dependencies := fixtureDeps()
	dependencies.Workflows.(*fixtureWorkflowService).saveDraftErr = &workflow.ValidationError{Issues: issues}
	recorder := performRequest(NewRouter(dependencies), http.MethodPut, "/api/workflows/w1", `{"draftRevision":1,"graph":{"schemaVersion":1,"nodes":[],"edges":[]}}`)
	if recorder.Code != http.StatusUnprocessableEntity { t.Fatalf("status=%d body=%s", recorder.Code, recorder.Body.String()) }
	var response ErrorResponse
	if err := json.Unmarshal(recorder.Body.Bytes(), &response); err != nil { t.Fatal(err) }
	if response.Code != "WORKFLOW_INVALID" || response.Message != "工作流校验失败" || !reflect.DeepEqual(response.Issues, issues) {
		t.Fatalf("response=%+v", response)
	}
}
```

测试先引用 `saveDraftErr`，本步不要修改 fixture，确保产生明确的编译失败。

- [ ] **Step 2: 运行测试确认失败**

```bash
CGO_ENABLED=0 go test ./apps/api/internal/httpapi -run TestWorkflowValidationErrorReturnsIssues -count=1
```

Expected: FAIL，`fixtureWorkflowService` 尚无 `saveDraftErr`。

- [ ] **Step 3: 增加最小错误注入点并运行回归**

同步修改 fixture：

```go
// 在 fixtureWorkflowService 的 validateErr 旁新增：
saveDraftErr error

func (service *fixtureWorkflowService) SaveDraft(context.Context, string, int64, domain.Graph) (domain.Workflow, error) {
	if service.saveDraftErr != nil {
		return domain.Workflow{}, service.saveDraftErr
	}
	return domain.Workflow{}, domain.ErrRevisionConflict
}
```

```bash
CGO_ENABLED=0 go test ./apps/api/internal/workflow ./apps/api/internal/httpapi -count=1
```

Expected: PASS；只修改测试 fixture，生产错误映射结构不变。

- [ ] **Step 4: 提交错误契约测试**

```bash
git add apps/api/internal/httpapi/router_test.go
git commit -m "test: lock workflow boundary error contract"
```

## Task 3: 前端边界纯逻辑

**Files:**
- Create: `apps/web/src/features/studio/workflowBoundaries.ts`
- Create: `apps/web/src/features/studio/workflowBoundaries.test.ts`
- Modify: `apps/web/src/features/studio/nodePlacement.ts:1-55`
- Modify: `apps/web/src/features/studio/nodePlacement.test.ts`

**Interfaces:**
- Consumes: `StudioNode`、`StudioEdge`、`FlowGraph`、`XYPosition`。
- Produces:

```ts
export type BoundaryKind = 'start' | 'end'
export interface BoundaryProblem { kind: BoundaryKind; issue: 'missing' | 'duplicate'; nodeIds: string[] }
export interface BoundaryDiagnosis { healthy: boolean; problems: BoundaryProblem[] }
export interface ProtectedDeletion extends FlowGraph { skippedBoundaryNodeIds: string[]; changed: boolean }
export interface BoundaryRepairSelection { keepStartId?: string; keepEndId?: string }
export interface BoundaryRepairPlan extends FlowGraph { addedNodeIds: string[]; removedNodeIds: string[]; removedEdgeIds: string[] }
export class BoundaryRepairSelectionError extends Error {}
export function isBoundaryNode(node: Pick<StudioNode, 'data'>): boolean
export function diagnoseWorkflowBoundaries(nodes: StudioNode[]): BoundaryDiagnosis
export function protectNodeChanges(changes: NodeChange<StudioNode>[], nodes: StudioNode[]): { changes: NodeChange<StudioNode>[]; skippedBoundaryNodeIds: string[] }
export function protectGraphDelete(nodes: StudioNode[], edges: StudioEdge[], deletedNodes: StudioNode[], deletedEdges: StudioEdge[]): ProtectedDeletion
export function buildBoundaryRepairPlan(nodes: StudioNode[], edges: StudioEdge[], selection: BoundaryRepairSelection, createBoundary: (kind: BoundaryKind, position: XYPosition) => StudioNode): BoundaryRepairPlan
export function safeBoundaryPosition(kind: BoundaryKind, nodes: StudioNode[]): XYPosition
export function boundaryMidpoint(nodes: StudioNode[]): XYPosition | undefined
```

- [ ] **Step 1: 写诊断、保护和修复计划失败测试**

```ts
const studioNode = (id: string, nodeType: string, position = { x: 0, y: 0 }): StudioNode => ({
  id, type: 'studio', position,
  data: { nodeType, typeVersion: '1', config: {}, ports: { inputs: [], outputs: [] }, issues: [] },
})
const studioEdge = (id: string, source: string, target: string): StudioEdge => ({ id, source, target, sourceHandle: 'out', targetHandle: 'in', data: {} })
const createBoundary = (kind: BoundaryKind, position: XYPosition) => studioNode(`${kind}-new`, kind, position)

it('混合删除跳过边界并保留其自动关联边', () => {
  const start = studioNode('start', 'start')
  const task = studioNode('task', 'template')
  const end = studioNode('end', 'end')
  const startTask = studioEdge('start-task', 'start', 'task')
  const taskEnd = studioEdge('task-end', 'task', 'end')
  const result = protectGraphDelete([start, task, end], [startTask, taskEnd], [start, task], [startTask, taskEnd])
  expect(result.nodes.map((node) => node.id)).toEqual(['start', 'end'])
  expect(result.edges).toEqual([])
  expect(result.skippedBoundaryNodeIds).toEqual(['start'])
})

it('只删除边界时保留边界和非显式选中的关联边', () => {
  const start = studioNode('start', 'start')
  const task = studioNode('task', 'template')
  const edge = studioEdge('start-task', 'start', 'task')
  expect(protectGraphDelete([start, task], [edge], [start], [{ ...edge, selected: false }])).toEqual({
    nodes: [start, task], edges: [edge], skippedBoundaryNodeIds: ['start'], changed: false,
  })
})

it('过滤 React Flow remove change，避免 onDelete 前先移除边界', () => {
  const start = studioNode('start', 'start')
  const task = studioNode('task', 'template')
  const result = protectNodeChanges([
    { type: 'remove', id: 'start' },
    { type: 'remove', id: 'task' },
  ], [start, task])
  expect(result.changes).toEqual([{ type: 'remove', id: 'task' }])
  expect(result.skippedBoundaryNodeIds).toEqual(['start'])
})
```

```ts
it.each([
  [['start', 'end'], []],
  [['end'], [['start', 'missing']]],
  [['start'], [['end', 'missing']]],
  [[], [['start', 'missing'], ['end', 'missing']]],
  [['start', 'start', 'end'], [['start', 'duplicate']]],
  [['start', 'end', 'end'], [['end', 'duplicate']]],
] as const)('诊断 %j', (types, expected) => {
  const diagnosis = diagnoseWorkflowBoundaries(types.map((type, index) => studioNode(`${type}-${index}`, type)))
  expect(diagnosis.problems.map((problem) => [problem.kind, problem.issue])).toEqual(expected)
  expect(diagnosis.healthy).toBe(expected.length === 0)
})

it('重复修复要求 keeper，并只移除其他节点及其关联边', () => {
  const nodes = [studioNode('start-a', 'start'), studioNode('start-b', 'start'), studioNode('end', 'end')]
  const edges = [studioEdge('a-end', 'start-a', 'end'), studioEdge('b-end', 'start-b', 'end')]
  expect(() => buildBoundaryRepairPlan(nodes, edges, {}, createBoundary)).toThrow(BoundaryRepairSelectionError)
  const before = structuredClone({ nodes, edges })
  const plan = buildBoundaryRepairPlan(nodes, edges, { keepStartId: 'start-a' }, createBoundary)
  expect(plan.removedNodeIds).toEqual(['start-b'])
  expect(plan.removedEdgeIds).toEqual(['b-end'])
  expect(plan.edges.map((edge) => edge.id)).toEqual(['a-end'])
  expect({ nodes, edges }).toEqual(before)
})

it('缺失边界使用安全坐标且空图中点只在唯一边界时存在', () => {
  const task = studioNode('task', 'template', { x: 120, y: 180 })
  expect(safeBoundaryPosition('start', [task])).not.toEqual(task.position)
  const start = studioNode('start', 'start', { x: 100, y: 100 })
  const end = studioNode('end', 'end', { x: 500, y: 100 })
  expect(boundaryMidpoint([start, end])).toEqual({ x: 300, y: 100 })
  expect(boundaryMidpoint([start, task, end])).toBeUndefined()
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
corepack pnpm@10.34.5 --filter @agent-studio/web test -- workflowBoundaries.test.ts nodePlacement.test.ts
```

Expected: FAIL，模块与导出尚不存在。

- [ ] **Step 3: 实现纯函数**

```ts
export function isBoundaryNode(node: Pick<StudioNode, 'data'>) {
  return node.data.nodeType === 'start' || node.data.nodeType === 'end'
}

export function diagnoseWorkflowBoundaries(nodes: StudioNode[]): BoundaryDiagnosis {
  const problems: BoundaryProblem[] = []
  for (const kind of ['start', 'end'] as const) {
    const nodeIds = nodes.filter((node) => node.data.nodeType === kind).map((node) => node.id)
    if (nodeIds.length === 0) problems.push({ kind, issue: 'missing', nodeIds })
    if (nodeIds.length > 1) problems.push({ kind, issue: 'duplicate', nodeIds })
  }
  return { healthy: problems.length === 0, problems }
}
```

`protectNodeChanges` 先过滤指向边界节点的 `remove` change，保证 React Flow 在调用 `onDelete` 前不会先改掉受控节点。`protectGraphDelete` 必须按以下规则实现：先从请求删除节点中移除边界；删除普通节点及其关联边；请求删除边若关联到被保护边界，仅在原 edge 的 `selected === true` 时删除，否则保留；只有节点或边实际变化时 `changed` 才为 true。`buildBoundaryRepairPlan` 必须 `structuredClone` 输入，缺失节点调用 `createBoundary`，重复节点要求 keeper，删除其他重复节点和其关联边，并返回稳定排序的影响 ID。

- [ ] **Step 4: 运行纯逻辑回归**

```bash
corepack pnpm@10.34.5 --filter @agent-studio/web test -- workflowBoundaries.test.ts nodePlacement.test.ts
```

Expected: PASS。

- [ ] **Step 5: 提交前端边界模型**

```bash
git add apps/web/src/features/studio/workflowBoundaries.ts apps/web/src/features/studio/workflowBoundaries.test.ts apps/web/src/features/studio/nodePlacement.ts apps/web/src/features/studio/nodePlacement.test.ts
git commit -m "feat: model protected workflow boundaries"
```

## Task 4: 修复横幅与空图引导组件

**Files:**
- Create: `apps/web/src/features/studio/BoundaryRepairBanner.tsx`
- Create: `apps/web/src/features/studio/BoundaryRepairBanner.test.tsx`
- Create: `apps/web/src/features/studio/CanvasEmptyGuide.tsx`
- Create: `apps/web/src/features/studio/CanvasEmptyGuide.test.tsx`
- Modify: `apps/web/src/features/studio/WorkflowCanvas.tsx:1-145`
- Modify: `apps/web/src/features/studio/WorkflowCanvas.test.tsx`

**Interfaces:**
- Consumes: Task 3 的 `BoundaryDiagnosis`、`BoundaryRepairSelection`、`boundaryMidpoint`。
- Produces:

```ts
interface BoundaryRepairBannerProps {
  diagnosis: BoundaryDiagnosis
  nodes: StudioNode[]
  edges: StudioEdge[]
  busy: boolean
  error: string
  onConfirm: (selection: BoundaryRepairSelection) => void | Promise<void>
}
interface CanvasEmptyGuideProps { position: XYPosition; onAdd: () => void }
```

`WorkflowCanvasProps` 新增 `emptyGuide?: { position: XYPosition; onAdd: () => void }`，使用 React Flow 12.11.2 已提供的 `ViewportPortal` 渲染，不向 `props.nodes` 注入伪节点。

- [ ] **Step 1: 写组件失败测试**

```tsx
const studioNode = (id: string, nodeType: string): StudioNode => ({
  id, type: 'studio', position: { x: 0, y: 0 },
  data: { nodeType, typeVersion: '1', config: {}, ports: { inputs: [], outputs: [] }, issues: [] },
})
const studioEdge = (id: string, source: string, target: string): StudioEdge => ({ id, source, target, sourceHandle: 'out', targetHandle: 'in', data: {} })

it('重复开始节点要求选择 keeper 并预览关联边', async () => {
  const onConfirm = vi.fn()
  const nodes = [studioNode('start-a', 'start'), studioNode('start-b', 'start'), studioNode('end', 'end')]
  const edges = [studioEdge('start-b-end', 'start-b', 'end')]
  const diagnosis = diagnoseWorkflowBoundaries(nodes)
  render(<BoundaryRepairBanner diagnosis={diagnosis} nodes={nodes} edges={edges} busy={false} error="" onConfirm={onConfirm} />)
  await userEvent.click(screen.getByRole('button', { name: '修复工作流边界' }))
  expect(screen.getByRole('dialog', { name: '修复工作流边界' })).toBeVisible()
  expect(screen.getByText(/将移除 1 个重复节点和 1 条关联连线/)).toBeInTheDocument()
  await userEvent.click(screen.getByRole('radio', { name: /保留 start-a/ }))
  await userEvent.click(screen.getByRole('button', { name: '确认修复' }))
  expect(onConfirm).toHaveBeenCalledWith({ keepStartId: 'start-a' })
})

it('空图引导只报告添加意图', async () => {
  const onAdd = vi.fn()
  render(<CanvasEmptyGuide position={{ x: 320, y: 180 }} onAdd={onAdd} />)
  await userEvent.click(screen.getByRole('button', { name: '在这里添加第一个节点' }))
  expect(onAdd).toHaveBeenCalledOnce()
})

it('缺失边界无需 keeper，busy 与错误状态保持可访问', async () => {
  const onConfirm = vi.fn()
  const nodes = [studioNode('end', 'end')]
  const { rerender } = render(<BoundaryRepairBanner diagnosis={diagnoseWorkflowBoundaries(nodes)} nodes={nodes} edges={[]} busy={false} error="" onConfirm={onConfirm} />)
  const trigger = screen.getByRole('button', { name: '修复工作流边界' })
  await userEvent.click(trigger)
  expect(screen.queryByRole('radio')).not.toBeInTheDocument()
  await userEvent.click(screen.getByRole('button', { name: '确认修复' }))
  expect(onConfirm).toHaveBeenCalledWith({})
  rerender(<BoundaryRepairBanner diagnosis={diagnoseWorkflowBoundaries(nodes)} nodes={nodes} edges={[]} busy error="保存失败" onConfirm={onConfirm} />)
  expect(screen.getByRole('alert')).toHaveTextContent('保存失败')
  expect(screen.getByRole('button', { name: '确认修复' })).toBeDisabled()
})

it('取消修复零回调并恢复横幅触发点焦点', async () => {
  const onConfirm = vi.fn()
  render(<BoundaryRepairBanner diagnosis={diagnoseWorkflowBoundaries([studioNode('end', 'end')])} nodes={[studioNode('end', 'end')]} edges={[]} busy={false} error="" onConfirm={onConfirm} />)
  const trigger = screen.getByRole('button', { name: '修复工作流边界' })
  await userEvent.click(trigger)
  await userEvent.click(screen.getByRole('button', { name: '取消' }))
  expect(onConfirm).not.toHaveBeenCalled()
  expect(trigger).toHaveFocus()
})

it('画布空图引导只上报添加意图，不产生节点或边变化', async () => {
  const onAdd = vi.fn()
  const onNodesChange = vi.fn()
  const onEdgesChange = vi.fn()
  render(<WorkflowCanvas {...baseProps} onNodesChange={onNodesChange} onEdgesChange={onEdgesChange} emptyGuide={{ position: { x: 300, y: 100 }, onAdd }} />)
  await userEvent.click(screen.getByRole('button', { name: '在这里添加第一个节点' }))
  expect(onAdd).toHaveBeenCalledOnce()
  expect(onNodesChange).not.toHaveBeenCalled()
  expect(onEdgesChange).not.toHaveBeenCalled()
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
corepack pnpm@10.34.5 --filter @agent-studio/web test -- BoundaryRepairBanner.test.tsx CanvasEmptyGuide.test.tsx WorkflowCanvas.test.tsx
```

Expected: FAIL，组件与 `emptyGuide` 属性尚不存在。

- [ ] **Step 3: 实现组件和 ViewportPortal 集成**

```tsx
{props.emptyGuide && (
  <ViewportPortal>
    <CanvasEmptyGuide position={props.emptyGuide.position} onAdd={props.emptyGuide.onAdd} />
  </ViewportPortal>
)}
```

`CanvasEmptyGuide` 根元素使用 `style={{ transform: translate(...) }}` 定位，按钮加入 `nodrag nopan`；`BoundaryRepairBanner` 使用现有 `ConfirmDialog` 的视觉模式，但重复节点选择与影响清单保留在独立 `role="dialog"` 中，确认按钮只有所需 keeper 都已选择时才启用。

- [ ] **Step 4: 运行组件回归**

```bash
corepack pnpm@10.34.5 --filter @agent-studio/web test -- BoundaryRepairBanner.test.tsx CanvasEmptyGuide.test.tsx WorkflowCanvas.test.tsx
```

Expected: PASS。

- [ ] **Step 5: 提交修复与引导组件**

```bash
git add apps/web/src/features/studio/BoundaryRepairBanner.tsx apps/web/src/features/studio/BoundaryRepairBanner.test.tsx apps/web/src/features/studio/CanvasEmptyGuide.tsx apps/web/src/features/studio/CanvasEmptyGuide.test.tsx apps/web/src/features/studio/WorkflowCanvas.tsx apps/web/src/features/studio/WorkflowCanvas.test.tsx
git commit -m "feat: add boundary repair and empty canvas guidance"
```

## Task 5: Studio 页面保护与非乐观修复编排

**Files:**
- Modify: `apps/web/src/features/studio/StudioPage.tsx:42-605`
- Modify: `apps/web/src/features/studio/StudioPage.test.tsx:220-330`
- Modify: `apps/web/src/features/studio/StudioCommandBar.tsx:7-66`
- Modify: `apps/web/src/features/studio/StudioCommandBar.test.tsx`
- Modify: `apps/web/src/features/studio/StudioQuickTools.tsx:5-34`

**Interfaces:**
- Consumes: Tasks 3–4 的诊断、保护、repair plan、横幅和 guide。
- Produces: `handleBoundaryRepair(selection): Promise<void>`；`boundaryBlocked` 统一控制画布写入、添加、测试和发布；删除跳过提示放入 `studio-canvas-status`。

- [ ] **Step 1: 写页面级失败测试**

在 `StudioPage.test.tsx` 中复用现有 `workflow`、`definitions` 和 `studioNode` fixture，并加入：

```tsx
function deferred<T>() {
  let resolve!: (value: T) => void
  let reject!: (reason?: unknown) => void
  const promise = new Promise<T>((resolvePromise, rejectPromise) => { resolve = resolvePromise; reject = rejectPromise })
  return { promise, resolve, reject }
}

const renderStudio = () => render(
  <MemoryRouter initialEntries={['/workflows/w1']}>
    <Routes><Route path="/workflows/:id" element={<StudioPage />} /></Routes>
  </MemoryRouter>,
)
const workflowWithoutStart = { ...workflow, draftGraph: { ...workflow.draftGraph, nodes: workflow.draftGraph.nodes.filter((node) => node.type !== 'start') } }
const workflowWithDuplicateEnd = { ...workflow, draftGraph: { ...workflow.draftGraph, nodes: [...workflow.draftGraph.nodes, { ...workflow.draftGraph.nodes.find((node) => node.type === 'end')!, id: 'end-duplicate' }] } }
```

```tsx
it('删除开始节点不会改图或保存', async () => {
  renderStudio()
  const start = await screen.findByTestId('node-start')
  fireEvent.click(start.closest('.react-flow__node')!)
  fireEvent.keyDown(document, { key: 'Delete', code: 'Delete' })
  await vi.waitFor(() => expect(screen.getByRole('status')).toHaveTextContent('开始和结束节点不可删除'))
  expect(api.saveWorkflow).not.toHaveBeenCalled()
  expect(screen.getByTestId('node-start')).toBeVisible()
})

it('复制或剪切边界节点不会创建副本或进入保存队列', async () => {
  renderStudio()
  const start = await screen.findByTestId('node-start')
  fireEvent.click(start.closest('.react-flow__node')!)
  await userEvent.keyboard('{Control>}c{/Control}{Control>}x{/Control}')
  expect(screen.getAllByTestId('node-start')).toHaveLength(1)
  expect(api.saveWorkflow).not.toHaveBeenCalled()
})

it('边界修复保存成功后才替换页面图', async () => {
  const savedWorkflowWithStart = { ...workflow, draftRevision: 2 }
  vi.mocked(api.getWorkflow).mockResolvedValue(workflowWithoutStart)
  const pending = deferred<Workflow>()
  vi.mocked(api.saveWorkflow).mockReturnValue(pending.promise)
  renderStudio()
  await userEvent.click(await screen.findByRole('button', { name: '修复工作流边界' }))
  await userEvent.click(screen.getByRole('button', { name: '确认修复' }))
  expect(screen.queryByTestId('node-start')).not.toBeInTheDocument()
  pending.resolve(savedWorkflowWithStart)
  expect(await screen.findByTestId('node-start')).toBeVisible()
})
```

```tsx
it.each([
  ['缺少开始节点', workflowWithoutStart],
  ['重复结束节点', workflowWithDuplicateEnd],
] as const)('%s 时阻塞普通写操作', async (_name, loaded) => {
  vi.mocked(api.getWorkflow).mockResolvedValue(loaded)
  renderStudio()
  await screen.findByRole('button', { name: '修复工作流边界' })
  expect(screen.getByRole('button', { name: '添加节点' })).toBeDisabled()
  expect(screen.getByRole('button', { name: '测试运行' })).toBeDisabled()
  expect(screen.getByRole('button', { name: '发布' })).toBeDisabled()
})

it('修复保存失败或冲突都保留原图和修复对话框', async () => {
  vi.mocked(api.getWorkflow).mockResolvedValue(workflowWithoutStart)
  vi.mocked(api.saveWorkflow).mockRejectedValueOnce(new APIError(503, 'TEMPORARY_UNAVAILABLE', '保存失败'))
  renderStudio()
  await userEvent.click(await screen.findByRole('button', { name: '修复工作流边界' }))
  await userEvent.click(screen.getByRole('button', { name: '确认修复' }))
  expect(await screen.findByRole('alert')).toHaveTextContent('保存失败')
  expect(screen.queryByTestId('node-start')).not.toBeInTheDocument()
  expect(screen.getByRole('dialog', { name: '修复工作流边界' })).toBeVisible()
})
```

混合删除测试从现有页面删除用例改造：fixture 含 start/template/end，选中 start 与 template 后按 Delete，断言保存 payload 仍有 start/end、没有 template 及其关联边，且 `api.saveWorkflow` 只调用一次。空图引导测试分别加载仅 start/end 和含 template 两种图，前者点击引导后出现节点库，后者断言引导不存在。

- [ ] **Step 2: 运行测试确认失败**

```bash
corepack pnpm@10.34.5 --filter @agent-studio/web test -- StudioPage.test.tsx StudioCommandBar.test.tsx
```

Expected: FAIL，页面仍允许删除边界且没有修复状态。

- [ ] **Step 3: 实现统一保护和修复事务**

```ts
const boundaryDiagnosis = useMemo(() => diagnoseWorkflowBoundaries(nodes), [nodes])
const boundaryBlocked = !boundaryDiagnosis.healthy

const handleDelete = (deleted: { nodes: StudioNode[]; edges: StudioEdge[] }) => {
  if (archived || versionLocked || boundaryBlocked) return
  const next = protectGraphDelete(nodes, edges, deleted.nodes, deleted.edges)
  if (next.skippedBoundaryNodeIds.length > 0) setCanvasNotice('开始和结束节点不可删除，已跳过工作流唯一节点')
  if (!next.changed) return
  setNodes(next.nodes)
  setEdges(next.edges)
  commit(next.nodes, next.edges)
}
```

`handleNodesChange` 必须先调用 `protectNodeChanges(changes, current)`，仅把过滤后的 changes 交给 `applyNodeChanges`；跳过边界时复用同一条非阻塞提示。`handleBoundaryRepair` 必须按以下顺序实现：生成 plan；设置 `repairing`；`saveQueue.current.enqueue(fromFlowGraph(plan.nodes, plan.edges))`；`await saveQueue.current.flush()`；成功后 `setNodes/setEdges`；失败时只设置 `repairError`；finally 清除 busy。不得在 flush 成功前调用 `setNodes` 或 `setEdges`。

- [ ] **Step 4: 接入命令条、guide 和全局禁用条件**

`WorkflowCanvas.readOnly`、`StudioQuickTools.disabled`、`StudioCommandBar.testDisabled`、发布按钮、连接和配置写入统一包含 `boundaryBlocked`。在 `StudioPage` 主体渲染 `BoundaryRepairBanner`。仅当 `boundaryDiagnosis.healthy`、普通节点数量为 0 且 `boundaryMidpoint(nodes)` 有值时传入 `emptyGuide`。

- [ ] **Step 5: 运行页面回归**

```bash
corepack pnpm@10.34.5 --filter @agent-studio/web test -- StudioPage.test.tsx StudioCommandBar.test.tsx WorkflowCanvas.test.tsx
```

Expected: PASS，且既有普通节点删除测试调整为删除 template 节点，不再把删除 start 当成正常行为。

- [ ] **Step 6: 提交页面编排**

```bash
git add apps/web/src/features/studio/StudioPage.tsx apps/web/src/features/studio/StudioPage.test.tsx apps/web/src/features/studio/StudioCommandBar.tsx apps/web/src/features/studio/StudioCommandBar.test.tsx apps/web/src/features/studio/StudioQuickTools.tsx
git commit -m "feat: protect and repair studio boundaries"
```

## Task 6: 画布骨架样式、E2E 与中文文档

**Files:**
- Modify: `apps/web/src/features/studio/studio.css:1-101,193-228`
- Modify: `apps/web/e2e/agent-studio.spec.ts:1-130`
- Modify: `docs/frontend-workbench.md`

**Interfaces:**
- Consumes: 完成后的修复横幅、空图引导、命令条和只读状态 data attribute/class。
- Produces: 390/768/1440px 无横向溢出的画布骨架；中文边界规则与恢复说明。

- [ ] **Step 1: 写 E2E 失败用例**

```ts
test('新建工作流保护唯一边界并显示首节点引导', async ({ page }) => {
  await createWorkflow(page, `boundary-${Date.now()}`, '边界保护')
  await expect(page.getByTestId('node-start')).toHaveCount(1)
  await expect(page.getByTestId('node-end')).toHaveCount(1)
  await expect(page.getByRole('button', { name: '在这里添加第一个节点' })).toBeVisible()
  await page.getByTestId('node-start').click()
  await page.keyboard.press('Delete')
  await expect(page.getByTestId('node-start')).toHaveCount(1)
  await expect(page.getByText(/不可删除/)).toBeVisible()
})
```

增加 API 级用例：读取当前 workflow revision，PUT 缺 start 图，断言 422、`WORKFLOW_INVALID` 和 `WORKFLOW_START_COUNT`。历史异常 UI 修复通过 Playwright route 拦截 GET 返回缺失边界，再检查修复 PUT 目标图；不向真实数据库写入非法草稿。

- [ ] **Step 2: 运行 E2E 确认新用例失败**

```bash
CGO_ENABLED=0 make test-e2e
```

Expected: 新增边界 UI 用例在样式/集成完成前 FAIL；已有 E2E 保持可运行。

- [ ] **Step 3: 完成样式和文档**

在 `studio.css` 增加 `.boundary-repair-banner`、`.boundary-repair-dialog`、`.canvas-empty-guide`、`.studio-canvas-notice`，复用语义 Token；按钮最小 44px；390px 使用全宽对话框；reduced-motion 禁用引导位移动画。`docs/frontend-workbench.md` 增加“开始/结束节点不可删除”“历史草稿修复”“修复失败/冲突恢复”三节，并写明修复不会自动重连。

- [ ] **Step 4: 运行前端与全量门禁**

```bash
corepack pnpm@10.34.5 --filter @agent-studio/web test
corepack pnpm@10.34.5 --filter @agent-studio/web typecheck
corepack pnpm@10.34.5 --filter @agent-studio/web build
CGO_ENABLED=0 make verify
CGO_ENABLED=0 make test-e2e
```

Expected: 全部 PASS；生产构建可保留基线已有的 Vite >500KB 提示，但不得出现新错误或新增依赖。

- [ ] **Step 5: 提交体验收口**

```bash
git add apps/web/src/features/studio/studio.css apps/web/e2e/agent-studio.spec.ts docs/frontend-workbench.md
git commit -m "test: close boundary canvas experience"
```

## Task 7: 代码审查、回归与 PR

**Files:**
- Review: PR1 全部分支差异

**Interfaces:**
- Consumes: Tasks 1–6 的所有提交。
- Produces: 无高优先级问题的 PR1 和可供 PR2 使用的主分支基线。

- [ ] **Step 1: 使用 `superpowers:requesting-code-review` 审查完整差异**

```bash
git diff --check origin/main...HEAD
git diff --stat origin/main...HEAD
git log --oneline origin/main..HEAD
```

审查重点：后端没有完整编译草稿；所有删除入口都经过保护；修复失败不替换页面图；修复不自动重连；无额外依赖与契约变更。

- [ ] **Step 2: 对每条有效审查意见先补失败测试再修复**

```bash
CGO_ENABLED=0 make verify
CGO_ENABLED=0 make test-e2e
```

Expected: 修复后全量 PASS。

- [ ] **Step 3: 使用 `superpowers:verification-before-completion` 做最终验证并确认工作树干净**

```bash
git diff --check origin/main...HEAD
git status --short
CGO_ENABLED=0 make verify
CGO_ENABLED=0 make test-e2e
```

Expected: 无未提交文件，所有命令 PASS。

- [ ] **Step 4: 推送并创建 PR**

先用 `apply_patch` 创建 `/tmp/agent-studio-v05b-pr1.md`，内容固定为：

```markdown
## 目标
- 后端草稿保存保证恰有一个开始节点和一个结束节点
- 前端保护边界节点，并为历史异常草稿提供显式修复
- 新建空画布提供非持久化首节点引导

## 安全边界
- 不调用完整 compiler 校验半成品草稿
- 重复边界由用户选择 keeper，关联边删除会明确预览且不自动重连
- 无数据库迁移、无 OpenAPI 变更、无新增依赖

## 验证
- `CGO_ENABLED=0 make verify`：PASS
- `CGO_ENABLED=0 make test-e2e`：PASS

## 后续
PR2 将在本 PR 合并后实现节点发现、放置预览和结构化节点卡片。
```

```bash
git push -u origin codex/v0.5-b-boundary-canvas-foundation
gh pr create --base main --head codex/v0.5-b-boundary-canvas-foundation --title "feat: protect workflow boundaries" --body-file /tmp/agent-studio-v05b-pr1.md
```
